import { FunctionalComponent, h } from "preact";

const Explicit: FunctionalComponent = () => {
  return (
    <span className="font-mono w-5 h-5 flex items-center justify-center font-bold text-gray-100 bg-gray-300 rounded-sm flex-shrink-0">
      E
    </span>
  );
};

export default Explicit;
